export const BREAKPOINTS = {
    sm: '(min-width: 768px)',
    md: '(min-width: 1001px)',
    lg: '(min-width: 1441px)',
} as const;

export const BREAKPOINTS_VALUES = {
    sm: 768,
    md: 1001,
    lg: 1441,
} as const;
